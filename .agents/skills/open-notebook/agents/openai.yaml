interface:
  display_name: "Open Notebook"
  short_description: "Repo-specific patterns and workflows for open-notebook"
  default_prompt: "Use the open-notebook repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true